export const DB_NAME = 'vsdesk-db';
export const DB_VERSION = 2;

export const STORE_NAMES = {
  bookmarks: 'bookmarks',
  users: 'users',
  profiles: 'profiles',
  workspaces: 'workspaces',
} as const;
